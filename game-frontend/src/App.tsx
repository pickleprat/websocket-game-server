// import { useState } from 'react'
// import { createClient, Session } from '@supabase/supabase-js'

import { JSX } from "react";
import "./App.css"

// let SUPABASE_API_URL : string = process.env.SUPABASE_API_URL as string;  
// let SUPABASE_ANON_KEY: string = process.env.SUPABASE_ANON_KEY as string;  

// if (SUPABASE_ANON_KEY && SUPABASE_API_URL) {
//   const supabase = createClient(SUPABASE_ANON_KEY, SUPABASE_API_URL)
// } 

export default function App() : JSX.Element {
  // const [session, setSession] = useState<Session | null> (null); 
  return (
    <>
    <div>Home page</div>
    <div></div>
    </>
  )
}
